{
    "compilerOptions": {
        "allowJs": true, // 이 파일안에 있는 JS파일도 허용하겠다
        "target" : "ES5",
        "outDir": "./dist", // 타입스크립트의 결과물이 어디에 들어갈 것인가
        "moduleResolution": "Node", // 모듈해석전략에 따라 classic 과 Node로 나뉜다.
        "lib": ["ES2015", "DOM", "DOM.Iterable"],
        "noImplicitAny": true,
        "typeRoots": ["./types","./node_modules/@types"],
        "strict": true,
        // "strictFunctionTypes": false,
    },
    "include": ["./src/**/*"] // 프로젝트를 기준으로 어떤 파일을 포함시킬것이냐, /**/* src아래의 모든 파일을 포함시키겠다
}